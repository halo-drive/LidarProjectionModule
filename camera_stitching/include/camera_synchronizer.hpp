#ifndef CAMERA_SYNCHRONIZER_HPP
#define CAMERA_SYNCHRONIZER_HPP

namespace camera_stitching {
    class CameraSynchronizer {
    public:
        CameraSynchronizer();
        bool initialize();
    };
}

#endif
